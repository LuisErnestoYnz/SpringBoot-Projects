package e1javabasedconfiguration.envios;

public interface SistemaCalculoDeCostoEnvioPorTransporte {
   // Método que recibe el peso del paquete y devolverá el costo total
   public void calcularCosto(double peso);
}
